# logwatcher

Smart log file monitor with real-time tail, pattern matching, stats, and colorized output

## Installation

```bash
pip install -r requirements.txt
```

## Usage

```bash
python main.py --help
```
